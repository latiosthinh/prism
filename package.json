{
  "name": "prism",
  "private": true,
  "workspaces": [
    "packages/*",
    "prism"
  ],
  "scripts": {
    "build": "npm run build --workspace=@prism/sdk && npm run build --workspace=prism",
    "dev": "npm run dev --workspace=prism",
    "test": "npm run test --workspaces"
  }
}
